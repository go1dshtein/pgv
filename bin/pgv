#!/usr/bin/env python2.7
import os
import sys
import logging
import pgv.config
import pgv.utils.options
import pgv.utils.utils
import pgv.utils.app


def main():
    options = pgv.utils.options.parse()
    config = pgv.config.parse(pgv.utils.utils.search_config(options.config))
    pgv.utils.utils.setup_logging(config.logging)
    logger = logging.getLogger(__name__)
    logger.debug("begin")
    logger.debug("arguments: %s", " ".join(sys.argv))
    try:
        pgv.utils.app.Application(config, options).run(options.command)
    except Exception, e:
        logger.exception(e)
        logger.debug("finish unsuccessfully\n\n")
        os._exit(1)
    logger.debug("finish successfully\n\n")


if __name__ == '__main__':
    main()
